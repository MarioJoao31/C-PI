#include <stdio.h>

int main(){
    // Syntax de for(contador = $; contador <=/>=/</> ; contador++/contador = contador + 1 )
    //ciclo (for) // int contador é a variavel que vai incremetar até ser maior que 10
    //contador++ <=> contador = contador +1 <=> contador += 1 
    for(int contador=0;contador<10;contador++){        
        printf(" %d \n", contador);//excreve o contador no terminal;
    }
    return 0;
}