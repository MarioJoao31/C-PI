#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void) {int i, j;
 for (i=0 ; i < 3 ; i++){
     printf(" i=%d\n",i);
    for (j=0 ; j < 3 ; j++){
        printf("J=%d\n",j);
        if (i!=1 && j!=1) 
            printf("%d%d.",i,j);
    }
    }

    (b = b + a) ==== (b += a)
     return 0 ;
}

